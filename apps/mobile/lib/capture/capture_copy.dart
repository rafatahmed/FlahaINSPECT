const captureTitle = 'Capture';
const tapToCapture = 'Tap to capture photo';
const saveLocallyLabel = 'Save locally';
const adjustPinLabel = 'Adjust pin on map';
const confirmPinLabel = 'Confirm';
const notesLabel = 'Additional notes (optional)';
const gpsImpreciseBanner =
    'GPS is less precise than 10 m. You can still save, or adjust the pin.';
const savedLocallyToast = 'Saved locally — pending sync';
const photoRequired = 'Take a photo before saving.';
const categoryRequired = 'Choose Defect, Normal, or Note.';
const storageWarnCopy = 'Less than 500 MB free. Capture may fail.';
const storageBlockCopy = 'Not enough storage (under 200 MB). Free space to capture.';
const archivedNoCapture = 'Archived — capture is disabled.';
const noteTooLong = 'Notes must be 4000 characters or fewer.';

const categoryDefect = 'defect';
const categoryNormal = 'normal';
const categoryNote = 'note';

const categoryLabels = {
  categoryDefect: 'Defect',
  categoryNormal: 'Normal',
  categoryNote: 'Note',
};
