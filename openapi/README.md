# OpenAPI

`flaha-inspect-v1.yaml` is introduced in **PR-04** (auth + error catalog) and extended through PR-08 (delta freeze).

`packages/api-client` is regenerated from this directory starting in **PR-05**.
