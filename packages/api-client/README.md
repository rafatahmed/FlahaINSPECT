# `@flaha/inspect-api-client`

TypeScript client for the Nest API.

- **PR-01:** hand stub (`PRODUCT`, `API_PREFIX`) so web can depend on the workspace package.
- **PR-05+:** regenerate from `openapi/flaha-inspect-v1.yaml` on each API PR.
